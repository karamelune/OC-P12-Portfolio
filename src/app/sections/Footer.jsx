import Link from 'next/link';

export const Footer = () => {
    return (
        <footer className="z-50 flex flex-col items-center justify-center w-full p-4 bg-gradient-to-l from-primary-50 via-default-50 to-primary-50">
            <p className="text-white">Made with 🩵 by me</p>
        </footer>
    );
};
